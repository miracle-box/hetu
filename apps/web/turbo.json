{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "env": ["NODE_ENV", "API_ROOT", "TEXTURE_STORE_ROOT"]
    },
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
